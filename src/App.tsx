import React from "react"

export default class App extends React.Component {
  render() {
    return (
      <div>
        <div>Now we have automatically busted the cache in the deployment step!</div>
      </div>
    )
  }
}
